package classes.android.dataStructure

import kotlin.collections.HashSet

class HashSet {
    //Actually uses a HashMap to store the hashset values,
    //item will be stored as a key in the hash map,
    //value will be stored as PRESENT dummy Object.
}